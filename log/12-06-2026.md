Today I wanted to do a bit of nftables, in the past i used iptables and so far i know iptables works with nftables beneath. To do so I'm going to read the official documentation [here](https://wiki.nftables.org/wiki-nftables/index.php/Main_Page)


First of all, i must create a rule for ssh -- i'm working with it so i dont want to lock myself out doing it. 

However, i had a problem that i dont know why yet. Each VM has 2 adapters: NAT and host-only. I dont know why but if i connect one the other deativate, to troubleshoot this problem i had to install `isc-dhcp-client` and run `dhclient <adapter>` wether it was enp0s3 or enp0s8. This way i could have the 2 adapters up


nft was installed by default but no enabled so we have to check what is going on with `systemctl status nftables`. Mine was disabled and inactive so i execute `systemctl enable nftables` and `systemctl start nftables` to begin

there is a file:

```
#!/usr/sbin/nft -f

flush ruleset

table inet filter {
        chain input {
                type filter hook input priority filter;
        }
        chain forward {
                type filter hook input priority filter;
        }
        chain output {
                type filter hook input priority filter;
        }
}
```

i backup this one just in case i have to put default setting and im going to work in another file to make a script.


en chain input 
policy drop;
ct state established, related accept
tcp dport 9176 accept 


